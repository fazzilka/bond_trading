# Bond Trading

Single-user web application for tracking bond lots and comparing planned yield with the
yield that can be fixed by selling at the best available bid.

The first working slice is under active development. See `docs/` for architecture,
calculation, MOEX, and import notes.
